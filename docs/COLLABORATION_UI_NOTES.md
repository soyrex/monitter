# Collaboration UI

The agent directory only lists saved agents. It displays collaboration profiles and enabled state from the persisted snapshot; it does not infer live availability. Task run detail renders collaboration records as supplied by the backend and links each record to the related task. Incoming messages use `senderAgentId` when available.
